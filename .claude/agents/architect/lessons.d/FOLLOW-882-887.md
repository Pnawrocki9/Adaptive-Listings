# 2026-08-07 · FOLLOW-882 + FOLLOW-887 (one PR) — gate-claim three-vocabulary sweep

- **What I decided:** Corrected sites 4–7 of the gating-ladder claim against HEAD
  (`route.ts:86,275`, `index.ts:827-829`): §E.4.6 pseudocode (`<`→`<=`, added the missing
  `signal_count` disjunction branch, withdrew the phantom "tunable per-tenant" knob — provenance
  traced to AUDIT-2026-06-04:1132, which assumed an `.env.example` entry never implemented), §D.7
  fallback rule 1 (`< 0.6`→`<= 0.6`), `follow-354.test.ts` header + test name ("SOLE gate"
  withdrawn, in agreement with the file's own AC-1), and — found live by the sweep, not named by any
  ticket — §E.1's similarity boundaries (`s < 0.6`→`s <= 0.6`, `< 0.85`→`<= 0.85` per
  `route.ts:323,328`: same permissive-boundary class, ADJACENT constant of the same function).
  Bumped MASTER_DESIGN v4.5→v4.6 rather than amending v4.5's changelog block: a merged PR's
  changelog block is a historical record — supersede, never edit, same as ADRs. Annotated (did not
  rewrite) the dated spec snapshot `TICKET-DESC-PIVOT-001-v1.7.1.md:963`, which still asserts a
  server-side confidence gate on `/adapt/description` that shipped code does not have.
- **Where a spec risked describing behavior with no owner:** the per-tenant threshold intent in
  §E.4.6 — deleting the sentence silently would have erased a possibly-wanted product knob; reported
  as stub-candidate FOLLOW-889 for the PM instead (I cannot mint stubs in FOLLOW_UPS.md).
- **Guardrails I'd add (two, both earned here):** (1) when sweeping a numeric gate claim, sweep the
  SIBLING CONSTANTS of the same function too — the seventh site was the similarity ladder three
  lines below the confidence gate, correct in the symbol vocabulary and wrong in the operator
  vocabulary; the claim class travels by function, not by constant. (2) The FOLLOW-448 pre-edit hook
  reads the MAIN checkout's HEAD, not the worktree's — when dispatched into a pre-created worktree,
  verify `.git/worktrees/<name>/HEAD` names the ticket branch (mine did:
  `architect/FOLLOW-882-887-gate-claim-sweep`) and record that verification, rather than either
  stopping wrongly or proceeding on faith.
- **Handoff note:** written as a `lessons.d/` fragment (Rule AG; PR #691 open, shared-tail append is
  the RETRO-260 CB-2 anti-pattern) AND pasted verbatim in the dispatch report per FOLLOW-888, so the
  PM lands it in the same commit as the deliverable.
