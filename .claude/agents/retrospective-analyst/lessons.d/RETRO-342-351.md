# RETRO-342..351 — analyst lessons (2026-09-22)

- **Date / RETRO:** 2026-09-22 / RETRO-342..351 (#922, #923, #925, #926, #927, #929, #928, #930,
  #931, #932).
- **A finding I almost missed and why:** the two surviving mutants in #923 (drop `|| force`;
  `flush(true)` → `flush()`). The test NAMED "a forced flush (hide/unload) re-sends a held batch"
  reads as coverage; it forces at `n=4`, a power of two that sends anyway. I only saw it because I
  ran the mutant instead of reading the test name. A test whose trigger coincides with the default
  path proves nothing about the branch it is named for; pick mutants that remove exactly the named
  branch.
- **An axis/chain I had to trace twice:** FOLLOW-1242's chain. First pass: SDK → ingest →
  ClickHouse, proven live by #930, "closed". Second pass, from the grader backwards: the harness
  read by wall time and graded delivered conversions RED — the gap had moved one hop, into a
  consumer that shares no symbol with `dispatchEvents()`. A producer's TIMING change must be traced
  to every reader that polls by time, and a consumer grep by symbol cannot find those (Candidate Z).
- **A meta-pattern in how gaps recur across agents:** merge trains. Three pairs/triples in this
  batch were cut from one base and squash-merged seconds apart (#925/#926/#927; #928/#929;
  #930/#931/#932). Every stale-at-merge sentence I found (SoT "pending FOLLOW-1249", #927 never
  seeing `lessons.d/`, FOLLOW-1252's "503 not identified") came from that shape. Rule AZ amendment 2
  already names it; what it lacks is an actor at merge time. Also: Rule AG violations persist
  because all ten agent definitions' `learning_hook` still say "append to lessons.md" — agents
  follow the file they execute, not the rule they are told about (FOLLOW-650).
- **Process note:** importing `differentiator-e2e.mjs` without setting
  `globalThis.FOLLOW1186_IMPORT_ONLY = true` RUNS the harness (by design) and writes
  `last-run.json`. I did that once in a probe, the run aborted at preflight, and I deleted the
  artefact. Set the global before importing.
