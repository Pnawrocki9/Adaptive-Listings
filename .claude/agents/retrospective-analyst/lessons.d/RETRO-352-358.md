# RETRO-352..358 — analyst lessons (2026-09-25)

- **Date / RETRO:** 2026-09-25 / RETRO-352..358 (#933–#939).
- **A finding I almost missed and why:** the duplicate FOLLOW-1256 ≡ FOLLOW-1275. The brief asked me
  to check the renumber for NUMBER collisions, and that check was clean. The CONTENT collision only
  showed up because I listed every stub header in the 1250–1276 range side by side. A renumber check
  needs both axes: the old numbers are gone, AND no new number re-files something already on `main`.
- **An axis/chain I had to trace twice:** consent enforcement after `consent-gate.ts` was deleted.
  First pass: `/api/adapt` has tests for consent-skip on GET and POST, so "single enforcement point
  with a test" looked true. Second pass, from the producer: the SDK never sends
  `consent_mode_enabled` (FOLLOW-1212) and halts before `/api/adapt` on `denied`/`pending`
  (`index.ts:374/390`). So the real enforcement point for consent is the SDK, and the route's tests
  exercise a branch nothing reaches. Opt-out and consent are two axes and have two different
  answers. A test proves the consumer; only a producer grep proves the wire.
- **A meta-pattern in how gaps recur across agents:** the removal PR (#938) found its own orphan
  (`/api/internal/schema`) and, obeying Rule AN, did not mint a number. Nobody else did either.
  Rules that forbid an actor from allocating have to name who allocates instead, or the orphan has
  no owner. Also: the session-injected CLAUDE.md I booted with was older than HEAD (it still showed
  "7 apps" and the 815 path). Read the file at HEAD before citing it.
- **Process note:** D8 (CEO 2026-09-24) is ruled but not yet implemented (FOLLOW-1259). I applied it
  in advance: no Rules, no P2/P3 stubs, P2/P3 findings listed in §7 as observations. The brief asked
  for retros on six docs-only PRs, which D8 will exclude once WP-1.2 ships.
