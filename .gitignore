# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Turbo
.turbo/

# Test coverage
coverage/
.nyc_output/

# Test artifacts
test-results/
playwright-report/
packages/sdk/corpus-report.json

# Environment files — use Doppler, never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*
!.env.example

# Editor/OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/settings.json
*.code-workspace

# Logs
*.log

# PM dispatch scratch: nohup worker logs and the per-ticket brief handed to them.
# Transient by construction — the durable content lands in backlog/QUEUE.md and
# backlog/FOLLOW_UPS.md. Ignored as a directory because only the .log half was covered
# above, so a `git add -A` would otherwise sweep the un-prettier-ed brief into CI.
.pm-logs/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
*.egg-info/
.eggs/
*.egg
MANIFEST
*.spec

# Terraform
.terraform/
.terraform.lock.hcl
*.tfplan
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example

# Cloudflare
.wrangler/
wrangler.toml.bak
# Worker secrets for `wrangler dev` (CLICKHOUSE_PASSWORD etc). Local-only, never committed
# — see docs/runbooks/LOCAL_PILOT_ENVIRONMENT.md §3.6 [FOLLOW-816].
.dev.vars
.dev.vars.*

# Size-limit
.size-limit/

# Misc
.cache/
tmp/
temp/
*.tmp
*.bak
.vercel

# Served SDK bundles — generated by apps/control-plane/scripts/copy-sdk-bundle.mjs on
# every build (ESC-047 / FOLLOW-808). Previously hand-built and committed, which let the
# served copy freeze for two months while ~76 SDK tickets merged. Never commit these.
apps/control-plane/public/sdk.js
apps/control-plane/public/estalara-detect.iife.js
