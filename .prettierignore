# dotenv files — no parser available for .env format
.env.example
*.env

docs/MASTER_DESIGN.md
# Append-only agent-generated retrospective log. Each entry is written by the
# retrospective-analyst subagent which cannot run prettier interactively. Long
# analysis lines (>100 chars) appear intentionally in retro content (inline code
# spans, table cells, quoted file paths). Exempt from auto-wrap enforcement.
backlog/RETROSPECTIVES.md
pnpm-lock.yaml
*.lock

# Built SDK bundle served as a Vercel static asset under admin.estalara.com/sdk.js.
# This is the minified IIFE output of `pnpm --filter @estalara/sdk build`; prettier
# would un-minify it on commit. [ESC-015]
apps/control-plane/public/sdk.js
