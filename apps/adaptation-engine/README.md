# estalara-adaptation-engine

Estalara adaptation engine — Modal serverless service for generating listing adaptation directives.

## Status

Placeholder — full implementation in TICKET-014.

## Development

```bash
cd apps/adaptation-engine
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest src/
```
