# estalara-archetype-pipeline

Estalara archetype pipeline — Modal daily batch job for buyer archetype clustering.

## Status

Placeholder — full implementation in TICKET-013.

## Development

```bash
cd apps/archetype-pipeline
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest src/
```
