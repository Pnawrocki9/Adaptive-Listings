# estalara-data-quality

Estalara data quality — Modal service for event validation and schema drift detection.

## Status

Placeholder — full implementation in TICKET-012.

## Development

```bash
cd apps/data-quality
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest src/
```
