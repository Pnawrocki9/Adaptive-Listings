{
  "name": "@estalara/decision-api",
  "version": "0.0.0",
  "private": true,
  "description": "Estalara adaptation decision Cloudflare Worker — p95 latency <80ms cached, <2000ms LLM path",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "wrangler dev",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist .wrangler"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "*",
    "vitest": "*",
    "wrangler": "^3.0.0"
  }
}
