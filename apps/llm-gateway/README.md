# estalara-llm-gateway

Estalara LLM gateway — Modal LiteLLM router for Claude Haiku/Sonnet calls.

## Status

Placeholder — full implementation in TICKET-013.

## Development

```bash
cd apps/llm-gateway
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest src/
```
