/**
 * @estalara/auth — JWT and API key utilities, Supabase Auth helpers.
 *
 * Full implementation in TICKET-009 (backend-engineer).
 * This module exports the AUTH_VERSION constant as a placeholder.
 *
 * @module @estalara/auth
 */

/** Current auth package version string. */
export const AUTH_VERSION = '0.0.0' as const;
