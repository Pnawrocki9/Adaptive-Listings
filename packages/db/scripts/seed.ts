/**
 * Seed script stub.
 *
 * Populate this file with dev/test fixture data as schemas are added in TICKET-021+.
 *
 * Run via:
 *   DATABASE_URL_DIRECT=<url> pnpm --filter @estalara/db tsx scripts/seed.ts
 */

console.log('Seed script: nothing to seed yet (schemas added in TICKET-021+).');
